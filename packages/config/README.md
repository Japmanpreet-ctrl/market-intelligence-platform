# @repo/config

Shared platform configuration for app metadata, constants, and environment
validation.

This package may validate configuration and expose typed configuration contracts.
It must not own feature-specific business rules.
