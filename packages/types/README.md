# @repo/types

Shared TypeScript contracts for platform-wide, non-domain-specific types.

Domain packages own their own contracts. This package should stay small and avoid
becoming a catch-all for feature implementation details.
